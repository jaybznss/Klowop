{
  "name": "klowop-server",
  "version": "1.0.0",
  "description": "Klowop backend — Sign in with Apple, Claude assistant proxy, StoreKit subscription verification, and per-user Plaid. Holds all secrets so the app never does.",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@apple/app-store-server-library": "^1.4.0",
    "better-sqlite3": "^11.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jose": "^5.9.6",
    "plaid": "^27.0.0"
  }
}

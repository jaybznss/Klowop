{
  "name": "klowop-server",
  "version": "1.0.0",
  "description": "Companion server for the Klowop iOS app — handles Plaid token exchange and data fetching so the Plaid secret never touches the phone.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "plaid": "^27.0.0"
  }
}
